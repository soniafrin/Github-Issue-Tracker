
    - 1️⃣ What is the difference between var, let, and const?
    Ans: These are the variables behave depends on scope, hoising and re-assingment. var and let can be re-assined but const cant. var can be redeclare but const and let can't be.

    - 2️⃣ What is the spread operator (...)?
    Ans: it helps copy and combined/merge element in array, objext, aurgument and strings

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Ans: These three function is based on arrays. Wherein map() create new arrays and return elements, filter() also create array with condition and forEach()just loop through elements and does not return anything.

    - 4️⃣ What is an arrow function?
    Ans:Arrow function is the shorter way of writing function also retrun by default if using without curly brase.

    - 5️⃣ What are template literals?
    Ans: Template literals is a method of writing string inside bactic instead quotations. it is powerful for writting strings dynamically also for any variables and expressions.